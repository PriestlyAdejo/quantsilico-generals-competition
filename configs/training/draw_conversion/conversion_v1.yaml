# Phase 9D CONVERSION — shared (CNN+graph both CONTACT_FAILURE).
# Minimal: one reward family (policy-visible contact) + one curriculum (train vs Expander).
schema_version: 1
kind: DRAW_CONVERSION_REWARD_CONFIG
version: conversion_v1
diagnosis: CONTACT_FAILURE
primary_reward_family: contact_visible_enemy_delta
curriculum_mechanism: train_vs_official_expander
training_opponent: official_expander
terminal:
  win: 1.0
  draw: 0.0
  loss: -1.0
  name: win1_draw0_loss-1
contact_shaping:
  enabled: true
  bonus_per_new_enemy_cell: 0.05
  max_per_step: 0.15
  max_episode_cumulative: 0.6
  name: contact_visible_enemy_delta
