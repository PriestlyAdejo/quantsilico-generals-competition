# Phase 9D matched four-arm pilot budgets (frozen before launch).
schema_version: 1
kind: DRAW_CONVERSION_PILOT_CONFIG
version: pilot_v1
prior_initial_env_steps: 1536
budgets:
  min_env_steps: 6144
  target_env_steps: 12288
  max_env_steps: 24576
  wall_clock_min_per_arm: 45
  chunk_env_steps: 512
  chunk_updates: 4
  rollout_steps: 128
  validation_every_chunks: 2
  min_validation_windows: 4
optimisation:
  lr: 0.0003
  clip: 0.2
  device: auto
treatment_effect:
  score_rate_epsilon: 0.05
validation:
  opponent: official_expander
  max_turns: 100
  both_seats: true
guards:
  stop_on_protocol_fault: true
  stop_on_fallback: true
  no_holdout_seeds: true
  no_git_during_run: true
