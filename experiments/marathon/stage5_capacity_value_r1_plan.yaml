kind: MARATHON_STAGE5_ROUND_PLAN
plan_id: STAGE5_CAPACITY_VALUE_R1
status: PREDECLARED
predeclared_at_utc: "2026-08-16T10:20:00Z"
corrected_at_utc: "2026-08-16T10:35:00Z"
correction_note: >-
  PRE-IMPLEMENTATION BASELINE RECONCILIATION (PLAN_CONFLICT surfaced and
  resolved honestly, no silent redesign): the EXECUTION_PLAN §11 ladder was
  authored assuming a CNN-era baseline, but the canonical competition_native_jax
  learner IS ALREADY a patch transformer (transformer_jax.py: 49 patches of
  3x3 + CLS + global token, 4 layers, emb 192, 4 heads, HL-Gauss 128-bin
  critic; lineage since commits 44bfc53/0dc8a57; every marathon screening
  and bridge run used it). T1 therefore needs no implementation - it is
  PRE-SATISFIED by the existing lineage, exactly like T0. The first OPEN
  ladder axis is T2 (legal temporal/global history features). R1 scope is
  corrected to T0+T1 pre-satisfied + T2 as the experimental arm.
stage: STAGE_5
ppo_semantics: UNCHANGED  # architecture research on the canonical learner; action-selection semantics unchanged
authority: >-
  CROSS_REGIME_BRIDGE_V1 escalation_rule_predeclared, triggered by EV-0054
  (RWB1 LAND gameplay arbiter: zero wins in 36 LAND games, controls all-draw,
  draw wall persists after B1+B2 with validated full-phase coverage). The
  triggering evidence is the B2 round outcome + its gameplay evaluation -
  exactly as predeclared, not future discretion.

question: >-
  Under the canonical PERSISTENT_EPISODE_REGIME_V1 at the canonical screening
  geometry, does isolating the first OPEN EXECUTION_PLAN §11 axis - T2 legal
  temporal history features (previous-step legal observation channels; the
  transformer itself is already the lineage baseline, correction_note) -
  produce external gameplay strength (win conversion) where every small-knob
  and reward-shaping intervention to date has produced only draws or losses?

background_constraints: >-
  Accumulated draw-wall evidence EV-0019/0035/0038/0039/0043/0053/0054: no
  trained candidate has converted wins against even legal_random. Calibration
  (EV-0053) proved the evaluator produces wins (official hunter 4/4). Value
  telemetry improvements (EV-0051) did not transfer. The binding constraint is
  therefore hypothesised to be representation/capacity, not optimiser knobs.

arms_predeclared:
  - arm: T0_CONTROL_EXISTING
    content: >-
      existing patch-transformer architecture, existing observation, HL-Gauss
      critic, reward none, persistent regime. SATISFIED PRE-EXISTING: the
      RWB1-A0-CONTROL-S1/S2 runs (240 updates, matched geometry,
      EV-0051/EV-0054) serve as T0; no recompute unless integrity flags.
  - arm: T1_PATCH_TRANSFORMER
    content: >-
      PRE-SATISFIED by the canonical lineage (correction_note): the learner
      already is the patch transformer of EXECUTION_PLAN §11 T1. Recorded,
      not re-run.
  - arm: T2_LEGAL_TEMPORAL_HISTORY
    content: >-
      the first OPEN ladder axis. Minimal isolated difference: append the
      PREVIOUS step's legal spatial observation as extra input channels
      (spatial 8 -> 16 planes; patch_proj input width doubles; ALL other
      architecture, heads, critic, masking, codec UNCHANGED). History lives
      in the rollout carry per environment, zeroed on reset/bootstraps;
      features are histories OF LEGAL OBSERVATIONS ONLY, so the fog-of-war
      legality gate (EV-0037) holds structurally; leak-guard suite extended
      with a history fixture. K=1 frame in R1 (K>1 is a later round, never
      bundled). Initialisation (declared BEFORE training): all shared layers
      warm-started from MARATHON_BASELINE_V0 exactly as T0; patch_proj is
      SHAPE-FORCED fresh (input width 8 -> 16 planes makes byte-identical
      warm start impossible); optimizer state and EMA re-initialised. This is
      the one recorded asymmetry vs T0; screening geometry/budget/seeds stay
      matched, and gameplay remains the arbiter.
  - later_arms_not_in_R1: [T3 critic changes (scalar vs distributional is
      MOOT - lineage already uses HL-Gauss; T3 becomes alternate critic
      parameterisations), T4 capacity scaling around any winner]
    note: each requires its own round plan building on R1 outcomes

protocol:
  regime: PERSISTENT_EPISODE_REGIME_V1 (canonical, EV-0049)
  geometry: canonical screening geometry 256 envs x 32 rollout, 240 updates
  seeds: [20260905, 20260907]
  funnel: >-
    PREDECLARE -> REGISTER -> SMOKE (2 updates) -> LEARNER HEALTH ->
    MATCHED_SCREENING (240 updates, same advancement rules as prior rounds -
    vloss reduction, validity, entropy non-collapse) -> GAMEPLAY ARBITER for
    any advancer
  gameplay_arbiter: >-
    identical fixed opponent set and protocol to the RWB1 arbiter
    (legal_random, heuristic_v0, heuristic_v1; 3 seat-swapped pairs each;
    1200-turn horizon UNCHANGED; serving sanity probe pre-game; reward-hack
    surveillance); the same adjudication logic applies - strength requires
    statistically above matched control AND above zero with the harness CS
  compute: local CPU first; if throughput is insufficient for the transformer
    at screening geometry, one bounded RunPod A40 round via the remote
    self-stop wrapper (prepaid credits only; RUNPOD-SPEND-2026-08-15 policy)

metrics_predeclared:
  - screening: vloss reduction vs T0 matched window, valid_share, entropy
  - decisive_share under persistent regime (terminal signal availability)
  - gameplay: win rate + CS per opponent, draw share, faults (must be ZERO)

adjudication_predeclared:
  - T1 advances ONLY through the gameplay arbiter; telemetry never promotes
  - a draw-wall repeat at T1 (zero wins, all draws) is recorded and triggers
    the NEXT predeclared step (T2 history features) - not ad-hoc knob turning
  - a T1 regression vs T0 is REJECTED with preserved artefacts
  - no post-hoc rule relaxation

parallel_lanes_unaffected:
  - BC-A-FULL-R1 (OFF_POLICY_AUXILIARY, experiment#bc-a-full-r1#abcfcd746ac7)
  - Stage 4B platform work (dependency-safe)

evidence_links: [EV-0049, EV-0051, EV-0053, EV-0054]
