# Model lifecycle and compatibility

Heuristic v2 is the submitted baseline. Learned CNN/graph artefacts remain research until promotion. Competition-size latency uses p50/p95/p99 from the latency gate.
