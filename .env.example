# This project currently requires no environment secrets.
# Never place submission credentials or GitHub tokens in this file.
# Copy to .env only if a future stage introduces local-only configuration.
